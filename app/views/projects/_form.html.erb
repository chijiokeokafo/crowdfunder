<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %> 

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
    <div class="field">
    <%= f.label :funding_goal %><br />
    <%= f.text_field :funding_goal %>
  </div>
  <div class="field">
    <%= f.label :start_date %><br />
    <%= f.date_field :start_date %>
  </div>
  <div class="field">
  <%= f.label :end_date %><br />
  <%= f.date_field :end_date %>
  </div>
    <div class="field">
    <%= f.label :category %><br />
    <%= f.select :category, Project.available_categories %>
  </div>
  <div id="rewards">
  <h3>Create your 3 pledge rewards</h3>
    <%= fields_for :rewards do |reward_fields| %>
      <div id='reward1'>
      <h4>5$ reward</h4>
        <%= reward_fields.label :description %>
        <%= reward_fields.text_area :description %>
      </div>
      <div id='reward2'>
      <h4>50$ reward</h4>
        <%= reward_fields.label :description %>
        <%= reward_fields.text_area :description %>
      </div>
      <div id='reward3'>
      <h4>500$ reward</h4>
        <%= reward_fields.label :description %>
        <%= reward_fields.text_area :description %>
      </div>
    <% end %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

